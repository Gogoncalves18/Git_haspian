/*Primeiro temos uma parte de importacao das bibliotecas:
Por exemplo, teremos importar atraves de #include <stdio.h> para importacao de entrada e saida de dados
O corpo principal ser'a construido por: void main() { } sem virgulas entre as linhas
*/
#include <stdio.h> 
#include <stdlib.h> /*Biblioteca com comandos do sistema*/
/*Eu posso iniciar o programa com void main(), neste caso nao preciso da um retorno como inteiro no final.
Posso iniciar apenas como main() ou posso iniciar com Int main() e no final tenho que escrever Return 0, 
faz parte da funcao.
*/
void main() {
    printf("Ola mundo!!!!"); /*Toda linha de cmd precisa de um ";" */
    system("pause");
}
